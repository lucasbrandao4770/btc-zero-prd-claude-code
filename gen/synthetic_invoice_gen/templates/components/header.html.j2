<div class="header">
    <div class="header-brand">
        <div class="logo-placeholder">{{ partner.display_name[:2].upper() }}</div>
        <div>
            <h1>{{ partner.display_name }}</h1>
            <div class="tagline">{{ partner.tagline }}</div>
        </div>
    </div>
    <div class="header-invoice-info">
        <div class="invoice-id">{{ invoice.invoice_id }}</div>
        <div>Order #{{ invoice.order_id }}</div>
        <div>{{ invoice.formatted_order_date }}</div>
    </div>
</div>
